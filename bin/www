#!/usr/bin/env node

var app = require('./../app');
var port = process.env.PORT || 3000;            // PORT on heroku, or 3000 on localhost dev

app.listen(port, function () {
    console.log('Listening on port ' + port);
});